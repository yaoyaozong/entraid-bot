# Azure AD / Entra ID Configuration (OAuth2)
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret

# OAuth2 Redirect URIs
# Option 1: Leave empty for dynamic detection (recommended for multi-domain deployments)
# The app will auto-detect based on request hostname
# REDIRECT_URI=
# POST_LOGOUT_REDIRECT_URI=

# Option 2: Explicit configuration (recommended for single domain or specific environments)
REDIRECT_URI=http://localhost:3000/auth/callback
POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# IMPORTANT: All redirect URIs must be registered in Azure Portal
# For multiple domains, register all of them:
# - http://localhost:3000/auth/callback
# - https://dev.example.com/auth/callback
# - https://staging.example.com/auth/callback
# - https://example.com/auth/callback

# Session Configuration
SESSION_SECRET=your-secret-key-change-in-production

# OpenAI API
OPENAI_API_KEY=your-openai-api-key

# MCP Server Configuration
MCP_SERVER_URL=http://localhost:3001

# Web Server Configuration
WEB_PORT=3000
